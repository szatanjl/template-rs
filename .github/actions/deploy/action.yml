name: Deploy
description: Deploy


inputs:
  ref:
    description: Commit ref
    default: ${{ github.sha }}
  env:
    description: Environment to deploy to
    required: true


runs:
  using: composite
  steps:
    - shell: bash
      run: echo TODO
